document.queryselector('header > h1').innerText = "inspector clouse"
document.querySelector('header > h2').innerText = "this is chief inspector clouse speakig on the phone"

function reDisplay() {
let windowSizes = `Window size is ${window.innerWidth} pixes wide by ${window.innerHeight} pixes tall.`
let offset = `window offset is ${window.screenX} from the left edge and ${window.screenY} pixels from the top of the display.`
 
let myWindow = document.querySelector('#myWindow').innertext = `${windowSizes} = \n ${offset}`
let date = new date();
let title = title;
}
reDisplay();